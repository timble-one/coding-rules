# TypeScript v2

Direct type-assertions are forbidden. Data allways has to be validated/parsed or checked with type-guards.  
For more complex parsing, npmjs.com/package/zod must be used.

Allways use curly braces for if/else-statements and use separate lines for the if/else bodies.